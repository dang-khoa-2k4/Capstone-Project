\chapter{Phương pháp đề xuất}
\label{ch:proposed_method}

\indent\textit{
    Chương này trình bày chi tiết về hai hướng tiếp cận để giải quyết
    bài toán hoạch định chuyển động cho rô-bốt. Đầu tiên là mô hình bài toán và các
    phương pháp phân hoạch không gian tự do thành tập hợp các vùng lồi (ACD, IRIS, VCC)
    — nền tảng hình học chung cho cả hai hướng tiếp cận. Tiếp theo là phương pháp lập
    kế hoạch chuyển động bằng Đồ thị các tập hợp lồi (GCS) với tham số hóa Bézier, và
    cuối cùng là phương pháp cải tiến sử dụng bài toán Điều khiển Tối ưu (OCP) kết hợp
    Direct Multiple Shooting và hàm cản logarit (log-barrier).
}

\input{experiments/prb_statement}
\input{experiments/cnx_decomp}

\section{Hướng tiếp cận 1: Hoạch định chuyển động bằng Graph of Convex Sets (GCS)}
\label{sec:gcs_approach}
\input{experiments/GCS}
\input{experiments/relaxation}
\input{experiments/gcs_bezier}
\section{Hướng tiếp cận 2: Hoạch định chuyển động sử dụng Optimal Control và Multiple Shooting}
\label{sec:ocp_dms_approach}

Để khắc phục các hạn chế của hướng tiếp cận hình học thuần túy, luận văn đề xuất mô hình hóa bài toán hoạch định chuyển động thành một bài toán Điều khiển Tối ưu (OCP) trên đồ thị các vùng lồi, sử dụng phương pháp direct multiple shooting. Mô hình này ghép hai khối lý thuyết: (i) Graphs of Convex Sets / network flow để chọn đường đi rời rạc qua các vùng an toàn lồi, và (ii) direct multiple shooting để mô hình hóa quỹ đạo liên tục trong mỗi vùng lồi. Phần ràng buộc an toàn hình học được hiện thực bằng cả hard constraint trên lưới hữu hạn lẫn hàm phạt log-barrier. Cấu trúc của mô hình hợp nhất được phân tích thành bốn lớp có thể xử lý tương đối độc lập (xem mục~\ref{subsec:layered_decomposition}), tạo nền tảng cho chiến lược giải sẽ được trình bày ở mục~\ref{subsec:solver_strategy}.

\input{proposed-method/ocp_dms}
